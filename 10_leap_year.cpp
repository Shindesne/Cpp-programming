#include <iostream>

using namespace std;

int main()
{
    int no;
    cout<<"enter the value";
    cin>>no;
    if(no%4==0)
    {
        cout<<"it is leap year";
    }
    else
    {
        cout<<"it is not  leap year";
    }
}
